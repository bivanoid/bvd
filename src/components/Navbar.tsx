import s from "./navbar.module.css";

export default function Navbar() {
    return (
        <nav className={s.nav}>
            <p>. :</p>
            <div className={s.logo}>bivanoid</div>
            <p>: .</p>
        </nav>
    )
}